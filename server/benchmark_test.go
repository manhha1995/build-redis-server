package server

import (
	"testing"
)

const MAX_REQUEST_SIZE = 2013

func benchmark_test(t *testing.T) {
	// var err error
	// listener, err = net.Listen("tcp", ":0")
	// if err != nil {
	// 	t.Error(err)
	// 	panic(err)
	// }
	// addr := listener.Addr().String()
	// tcp := net.Dial("tcp", addr)
	// for i := 0; i < MAX_REQUEST_SIZE; i++ {

	// }
}
